<div class="bsl-container">
  <div class="card shadow">
    <div class="d-flex justify-content-between align-items-center py-3 px-4">
      <h5>Organisation Admin</h5>
      <%= link_to new_organisation_organisation_admin_path, class: 'btn btn-md font-600 text-capitalize font-12 ml-auto dark-btn ml-2' do %>
        <i class="fa fa-plus-circle mr-1" aria-hidden="true"></i>
        Add New
      <% end %>
    </div>
    <div class="table-responsive">
      <% if @org_admins.present? %>
        <table class="table custom-table font-14 m-0">
          <thead>
            <tr>
              <th>Name</th>
              <th>Email</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            <% @org_admins.each do |org_admin| %>
              <tr>
                <td class="text-dark"><%= org_admin.name %></td>
                <td><%= org_admin.email %></td>
                <td>
                  <%= link_to 'View', organisation_organisation_admin_path(org_admin), class: 'btn dark-btn btn-sm' %>
                  <%= link_to 'Edit', edit_organisation_organisation_admin_path(org_admin), class: 'btn dark-btn btn-sm' %>
                  <%= link_to 'Delete', organisation_organisation_admin_path(org_admin), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn dark-btn btn-sm' %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      <% else %>
        <div class="alert alert-primary m-0" role="alert">
          No Organisation Admins are created yet!
        </div>
      <% end %>
    </div>
  </div>
</div>
