<div class="dropdown body-color-dropdown mr-2 ">
  <button class="border-0 p-3 bg-body-color dropdown-toggle w-100 shadow-none d-flex align-items-center justify-content-between" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
    <span class="d-flex flex-column align-items-start">
      <h6 class="text-capitalize font-12 font-xxl-14 mb-1">
        <span class="font-600 text-capitalize">Tiers .</span>
        <span class="text-light-grey font-600 tiers-count">0</span>
      </h6>
      <!-- <span class="text-capitalize font-12 font-xxl-14 text-light-grey font-600">View all</span> -->
    </span>
  </button>
  <div class="dropdown-menu down-drop z-index-12 h-420-with-scroll" aria-labelledby="dropdownMenuButton">
    <!-- <div class="form-group dropdown-item pl-3 m-0">
      <label for="tier-all" class="custom-checkbox d-flex m-0">
        <span class="font-12 text-black text-capitalize">View All</span>
        <%#= check_box_tag 'filters[tier_ids][]', 'all', checkboxes_handler(params, "tier_ids", "all"), id: "tier-all", class: 'form-control tiers', onclick: 'selectCall();RailsUJS.fire(this.form, "submit")' %>
        <span class="transparent-checkmark"></span>
      </label>
    </div> -->
    <% BusinessServiceLine.tiers.keys.each do |tier| %>
      <div class="form-group dropdown-item pl-3 m-0">
        <label for="tier<%= tier %>" class="custom-checkbox d-flex m-0">
          <span class="font-12 text-black text-capitalize">
          <%= tier.titleize %></span>
          <%= check_box_tag 'filters[tier_ids][]', tier, checkboxes_handler(params, "tier_ids", tier), id: "tier#{tier}", class: 'form-control tiers', onclick: 'RailsUJS.fire(this.form, "submit")' %>
          <span class="transparent-checkmark"></span>
        </label>
      </div>
    <% end %>
  </div>
</div>
