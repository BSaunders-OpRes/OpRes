<%= form_tag organisation_journey_path('institution-unit'), remote: true do %>
  <div class="card-body w-75 mx-auto font-14 mt-4 border">
    <div class="row">
      <div class="col-sm-12">
        <p class="font-14 body-bg-color p-2">
          <strong>Question:</strong> You selected the following regions; <%= organisational_unit.children.pluck(:region).join(', ') %>. Please can you tell us which countries you operate in across these regions? These can be amended later on if you don't manage to capture them all right now.
        </p>
      </div>
      <div class="col-sm-12 mt-3">
        <% organisational_unit.children.each do |regional_unit| %>
          <div class="col-12 p-0">
            <div class="faded-popover-wrapper">
              <div class="faded-popover-title bg-dark text-white p-2 rounded d-inline-block country-box text-center">
                <span class="d-inline-block mr-1 faded-popover-expandable-text">Select Country :</span>
                <span class="d-inline-block"><%= regional_unit.region %></span>
                <i class="fa fa-expand"></i>
              </div>
              <div class="faded-popover-content d-none">
                <div class="d-flex justify-content-between align-items-center px-2 mb-3">
                  <span class="faded-popover-close bg-white btn py-1 px-1 font-11 font-600">Confirm</span>
                  <i class="fa fa-times-circle-o close-i faded-popover-close"></i>
                </div>
                <% country_units = regional_unit.children %>
                <% Unit.countries_selection_by_region(regional_unit.region).each do |alphabet, countries| %>
                  <div class="ml-2 mb-2 pb-2 font-16 border-bottom">
                    <%= alphabet %>
                  </div>
                  <% countries.each do |country, code| %>
                    <div class="form-check d-flex align-items-center mr-2 form-group check-field">
                      <label class="form-check-label text-capitalize font-13 custom-checkbox" for="<%= "country-#{country.parameterize}" %>">
                        <%= country %>
                        <%= check_box_tag "countries[#{regional_unit.id}[#{country}]]", code, country_units.pluck(:country).include?(code), class: 'form-check-input mt-0 shadow-none', id: "country-#{country.parameterize}" %>
                        <span class="checkmark shadow-sm rounded"></span>
                      </label>
                    </div>
                  <% end %>
                <% end %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
  <div class="w-75 mx-auto mt-4">
    <div class="progress custom-progress">
      <div class="progress-bar progress-bar" role="progressbar" style="width: 40%" aria-valuenow="10" aria-valuemin="0" aria-valuemax="100"></div>
    </div>
    <div class="d-flex justify-content-between font-12">
      <span>Step 3</span>
      <span>40%</span>
    </div>
  </div>
  <div class="card-body w-75 d-flex p-0 mx-auto justify-content-between my-4">
    <%= link_to 'Back', organisation_journey_path('regional-unit'), class: 'btn dark-btn text-capitalize w-25 rounded-0' %>
    <%= submit_tag 'Next', class: 'btn primary-btn text-capitalize w-25 rounded-0' %>
  </div>
<% end %>
