<% if @products %>
  $("#products").html('<%= escape_javascript render "organisation/business_service_lines/partials/check_box", path: "products_ids[]", options: @products&.map{|product| [product.name, product.id] } %>');
<% else %>

<% end %>

<% if @channels %>
  $("#channels").html('<%= escape_javascript render "organisation/business_service_lines/partials/check_box", path: "channel_ids[]", options: @channels&.map{|channel| [channel.name, channel.id] } %>');
<% else %>

<% end %> 
